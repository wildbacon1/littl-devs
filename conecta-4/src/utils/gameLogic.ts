// utils/gameLogic.ts
export const checkForWinner = (
    board: (null | "R" | "Y")[][]
): null | "R" | "Y" => {
    // Implementa la lógica para comprobar si hay un ganador
    return null; // Devuelve 'R' o 'Y' si hay un ganador, de lo contrario null
};